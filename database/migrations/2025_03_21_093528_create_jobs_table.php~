<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('jobs', function (Blueprint $table) {
            $table->id();                       // Auto-incrementing primary key
            $table->string('title');            // Job title
            $table->text('description');        // Job description
            $table->string('company')->nullable(); // Company name (optional)
            $table->string('location')->nullable(); // Job location (optional)
            $table->decimal('salary', 10, 2)->nullable(); // Salary (e.g., 50000.00)
            $table->enum('status', ['open', 'closed', 'pending'])->default('open'); // Job status
            $table->timestamps();               // created_at and updated_a  // Adds created_at and updated_at
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('jobs');
    }
};
